import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    gridContainer: {
        maxWidth: '1000px',
        width: 'auto',
        marginLeft: 'auto', 
        marginRight: 'auto'
    }
}))